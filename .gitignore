# Sous-produits generes par l'organisme (pas du code source)
data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Secrets : la cle Claude passe par variable d'env / credentials, jamais commitee
.env

# OS / editeurs
.DS_Store
Thumbs.db
.vscode/
.idea/

# Docs internes (IP, strategie, sessions)
docs/esoleau-*
docs/HANDOFF-*
docs/LANCEMENT-*

# Secrets accidentellement notes en clair (mot de passe root, cles SSH, jetons)
docs/Neogen-vps.txt

# Videos tuto : fichiers volumineux (jusqu'a 230 Mo, > limite GitHub 100 Mo)
# Deployees hors-git via SCP direct vers static/video_tuto/ sur le VPS.
static/video_tuto/
docs/deploy_webhook.log
